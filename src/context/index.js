import React from "react";

// const GlobalContext = React.createContext({age:'100'}); because init value it doesn't matter becase i deplicate in provider this context i put value for it
const AuthContext = React.createContext();

export default AuthContext;
